---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const {
  as: Tag = "div",
  class: className = "",
} = Astro.props;
---

<Tag
  class:list={[
    "mx-auto w-full max-w-[90rem] px-6 md:px-8 lg:px-12 xl:px-16",
    className,
  ]}
>
  <slot />
</Tag>